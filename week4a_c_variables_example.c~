/* The file week4a_c_variables_example.c
   Was presented in COMP 206 on Sept 30, 2015
   
   Make sure that you can read this code and predict the value
   of each variable that's printed in the end.
   
   Pay special attention to davesc which is a character, printed
   with format %d. Ensure you know what that means.
   
   Author: David Meger
*/

#include <stdio.h>

int a = 1;

void foo()
{
	a = 3;
}

int main(int argc, char *argv[] )
{
	char *x = "joe";

	printf( "string value: %s.\n", x );

	int b, c, d = 4;

	foo();

	b = 2;
	c = a + 2;
	d = a + 3;

	char davesc = 'a';
	printf( "Variable values are a: %d, b: %d, c: %d, d: %d, davesc: %d.\n", a, b, c, d, davesc );

	return 0;
}

